/**
 * A constant representing the current year.
 */
const CURRENT_YEAR = new Date().getFullYear()

/**
 * A constant representing the duration for fade-in and
 * fade-out animations in milliseconds.
 */
const FADE_IN_OUT_DURATION_MS = 3000

/**
 * A constant representing the duration for CSS fade-in and
 * fade-out animations in milliseconds.
 */
const CSS_FADE_IN_OUT_DURATION_MS = 500

export { CURRENT_YEAR, FADE_IN_OUT_DURATION_MS, CSS_FADE_IN_OUT_DURATION_MS }
